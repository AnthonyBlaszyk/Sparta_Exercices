function tellFortune(job, city, name, year) {
  // Code the function here.
  console.log(`You will be ${job} in ${city}, and married to ${name} in ${year} years.`);
}

// Do not remove last lines, it is for tests
module.exports = tellFortune;
